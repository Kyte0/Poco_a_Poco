<br>
<br>
<br>

<div class="container">
<h1 class="p-5"> Welcome <%= current_user.first_name %> <%= current_user.last_name %> </h1>
<% current_user.photos.each do |photo| %>
  <%= cl_image_tag photo.key, height: 300, width: 400, crop: :fill %>
<% end %>

<div>
<h1> Your classes: </h1>
 <% current_user.classes.each do |lesson| %>
 <div>
 <h4> You have created a class to teach the <%= lesson.instrument %> for €
<%= lesson.price %> </h4>
</div>
<% end %>
</div>

<div>
<h1> Your bookings: </h1>
<% current_user.lessons.each do |lesson| %>
<div>
 <h4> You booked a lesson with <%= lesson.user.first_name %> to learn the <%= lesson.instrument %> </h4>
 <h1></h1>
 </div>
<% end %>
</div>
</div>
<br>
<br>
<br>
